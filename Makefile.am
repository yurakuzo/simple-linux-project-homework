SUBDIRS = src

clean-all:
	rm -rf Makefile config.h config.log config.status autom4te.cache
	find . -name '*.o' -delete
	find . -name '*.lo' -delete
	find . -name '*.la' -delete
	rm -f /usr/local/bin/simple_program
	rm -f /usr/local/include/header.h

